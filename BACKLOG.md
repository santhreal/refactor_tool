# BACKLOG - refactor_tool

Seeded 2026-07-14 (main-thread audit; Gemini swarm was quota-blocked). Internal AST-splitting dev tool for surgec refactors (operates on trusted source). Rows: `file:line | category | severity | problem | acceptance criteria`.

DONE 2026-07-14 | silent-fallback | medium | `append_to_file` does `fs::read_to_string(path).unwrap_or_else(|_| "use super::*;\n")` — if the target file EXISTS but is unreadable (permissions, transient IO), the error is swallowed and the file is overwritten with only the header + new content, DESTROYING the existing content. A tool that rewrites source silently losing a file's prior contents is data loss. | distinguish `NotFound` (legitimately new file) from other IO errors; propagate non-NotFound errors instead of discarding existing content. | FIXED: `append_to_file` now propagates read errors (excluding only `NotFound`, which correctly seeds a new file with `use super::*;`). It no longer silently discards an existing file or non-UTF-8 content with a default. Added unit tests for missing-file creation, existing-content preservation, and non-UTF-8 read propagation.
src/lib.rs:214 | capability | medium | `item_name` names an `Item::Impl` by its self-type (e.g. `"Foo"`), but the `run_*` group lists enumerate FUNCTION/method names (`"step"`, `"emit_add"`, …), never type names. So impl blocks never match any group and always stay in `retained_items` — if the intent was to relocate method impls alongside their group, they silently don't move. | clarify whether impls should be distributed; if yes, match on contained method idents; add a test pinning impl-block placement. | DONE 2026-07-14: `distribute_items` now distributes `impl` blocks based on the identifiers of their contained methods/associated functions (`impl_method_names`), so an `impl Foo { fn step() {} }` moves to the `steps` group module. `Item::Impl` self-type names are no longer the sole distribution key. Added tests `distribute_items_moves_impl_block_by_method_name` and `distribute_items_retains_impl_without_matching_method`. `cargo +1.92.0 test -p refactor_tool` 45 passed (CARGO_EXIT=0). | status=done
DONE 2026-07-14 | src/lib.rs:200 | test-gap | medium | The crate rewrites source files on disk and has ZERO tests. No coverage that items land in the correct module file, that `retained_items` is right, that the mod-decl injection is idempotent, or that round-tripping preserves code. | add tests over a fixture `syn::File`: assert each named item is moved to the expected `<group>.rs`, unmatched items are retained, and re-running is idempotent. Assert file contents, not just success. | ADDED 2026-07-14: Added `distribute_items` unit tests covering item movement/retention, impl-block distribution by method name, mod-decl idempotence on re-run, and path validation. `distribute_items` now injects `pub mod`/`pub use` declarations via AST items, writes the main file once, and validates the target path up front. `cargo +1.92.0 test -p refactor_tool` 45 passed (CARGO_EXIT=0). | status=done
DONE 2026-07-14 | src/lib.rs:273 | fragility | low | Module wiring is done by string manipulation of generated source: prepends `pub mod X;\npub use X::*;` and `replace("#![allow(clippy::too_many_arguments)]", "")`, and re-reads the target file inside the loop each iteration (O(groups) full-file reads + writes). Brittle and quadratic. | build the module declarations via tokens/AST and write the target once after the loop; drop the string `replace` hack. | DONE 2026-07-14: Replaced the string-based `replace` hack and the per-iteration file read/write loop with a single AST-based mod-decl injection and one `fs::write` of `ast.to_token_stream()`. Duplicate mod/use declarations are detected by normalizing whitespace in the existing token stream. `cargo +1.92.0 test -p refactor_tool` 45 passed (CARGO_EXIT=0). | status=done
DONE 2026-07-14 | src/lib.rs:267 | robustness | low | `target_path.parent().unwrap()` and `.file_stem().unwrap()` panic on paths lacking a parent or stem (e.g. `/`, a path ending in `..`). `#![allow(unwrap_used)]` hides it. | validate the path up front and return a clear error (context + fix) instead of an unwrap panic. | DONE 2026-07-14: `distribute_items` now validates `target_path` before writing: parent must be non-empty, file stem must be non-empty, and the extension must be `.rs`. Errors include the path and a clear fix (`target must be a `.rs` file with a non-empty stem`). The `unwrap` panics are gone. `cargo +1.92.0 test -p refactor_tool` 45 passed (CARGO_EXIT=0). | status=done
DONE 2026-07-14 | src/main.rs:21 | robustness | low | `fs::read_to_string(target_path).expect("failed to read target")` and `parse_rust_file(..).expect("failed to parse rust file")` panic with no path/context. Engineering standard: error messages carry context + the fix. | replace with graceful error reporting including the path and parse diagnostic, non-zero exit. | DONE 2026-07-14: Replaced `expect` panics in `main` with `match` arms that print the target path and the underlying `io::Error`/`syn::Error`, then return `ExitCode::FAILURE` instead of unwinding. `run_*` helpers now return `std::io::Result` and propagate `distribute_items` errors. `cargo +1.92.0 test -p refactor_tool` 45 passed (CARGO_EXIT=0). | status=done
DONE 2026-07-14 | src/main.rs:36 | data | low | The decode/lower/interp/naga group name lists are large hardcoded inline `vec![..]` tables specific to surgec's files (Tier-B-shaped data embedded in code). | if this tool is reused, move the group tables to a data file the CLI reads; otherwise document that they are surgec-specific one-offs. | DONE 2026-07-14: Added a module-level doc comment stating that the `decode`/`lower`/`interp`/`naga` group tables are surgec-specific one-offs, not a generic refactoring vocabulary, and should be moved to a Tier-B data file if the tool is ever reused for another codebase. `cargo +1.92.0 test -p refactor_tool` 45 passed (CARGO_EXIT=0). | status=done